@model IEnumerable<AviaSales.Models.FlightTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Flight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Flight.FlightNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ticket.TicketId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FlightTicketId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FlightTicketId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FlightTicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
